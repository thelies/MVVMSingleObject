# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TestVariable' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestVariable
  pod 'RealmSwift'
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'RxRealm'
  pod 'RxDataSources'

  target 'TestVariableTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestVariableUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
